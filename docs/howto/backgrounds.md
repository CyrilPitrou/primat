# Custom backgrounds and `extra_rho`

:::{note}
*(stub — FABLEADVICE O-3)* Migrate the custom-background workflow: passing a
`background=` object or `extra_rho=` extra energy-density term to
`PRIMAT.__init__` / `primat.backend.run_bbn` (Python-only today — the C backend
raises for `force_backend="c"` and falls back to Python for `"auto"`), and the
Neff / expansion-rate implications.
:::
