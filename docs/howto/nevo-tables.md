# Custom NEVO (neutrino-decoupling) tables

:::{note}
*(stub — FABLEADVICE O-3)* Migrate from CLAUDE.md's "Advanced: custom NEVO
tables" section: the `nevo_file` / `nevo_spectral_file` / `nevo_grid_file` /
`nevo_file_prefix` overrides, their expected column/length shapes, the early
validation in `PRIMATConfig.__init__`, the `external_scale_factor` mode, and
how these feed the n↔p weak-rate cache fingerprint.
:::
