# CLASS / CAMB background tables

:::{note}
*(stub — FABLEADVICE O-3)* Migrate the workflow for driving primat from an
externally-computed background (CLASS/CAMB thermodynamics tables) and the
`primat_tools` Cobaya wrapper pointer (the wrapper itself lives in the separate
[`primat_tools`](https://github.com/CyrilPitrou/primat_tools) repo). See also
the docs-site "Using primat in MCMC chains" how-to called out in FABLEADVICE.
:::
