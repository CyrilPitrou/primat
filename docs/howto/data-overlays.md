# Data overlays: `data_dir` and `user_nuclear_dir`

:::{note}
*(stub — FABLEADVICE O-3)* Migrate from CLAUDE.md's "Rates directory resolution
(overlay)" section: `data_dir` (full replacement of the `primat/data/` tree)
vs `user_nuclear_dir` (additive nuclear networks/tables overlay), the
`resolve_rates_path` lookup order, the wired call sites, and the C-side
equivalents (`--data_dir` / `CPRIMAT_DATA_DIR`).
:::
