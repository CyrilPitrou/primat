# Custom networks (GUI zip and API)

:::{note}
*(stub — FABLEADVICE O-3)* Migrate from `EXTENDING.md` and CLAUDE.md's "Custom
networks (GUI)" section: the `primat-gui` "Create custom network" / "Import
custom network" workflow, the re-importable `.zip` format (reinterpolated
on-grid tables), and the programmatic `custom_network=` path through
`primat.backend.run_bbn` (supported on both backends).
:::
