# `primat.sensitivity` — logarithmic sensitivity tables

`sensitivity_table()` finite-differences full BBN solves into a matrix of
∂ln(observable)/∂ln(parameter). {doc}`../howto/sensitivity` explains the
three kinds of target and how to keep an additive one an elasticity; this
page is the signature-level reference.

```{eval-rst}
.. automodule:: primat.sensitivity
   :members:
   :undoc-members:
   :show-inheritance:
```
