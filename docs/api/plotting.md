# `primat.plotting` — plotting helpers

Plotly figures over an `EvolutionResult`: abundance evolution, the final
abundance panel, and the helpers the GUI and the notebooks share. Needs the
`plots` extra (`pip install "primat[plots]"`).

```{eval-rst}
.. automodule:: primat.plotting
   :members:
   :undoc-members:
   :show-inheritance:
```
