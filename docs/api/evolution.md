# `primat.evolution` — time-evolution schema

The module docstring below is the authoritative contract for the
time-evolution TSV: which columns exist, in what order, and what they mean.
Both backends' writers conform to it, so a file written by either is
`load_evolution`-loadable. {doc}`../howto/output` is the task-oriented
version of the same material.

```{eval-rst}
.. automodule:: primat.evolution
   :members:
   :undoc-members:
   :show-inheritance:
```
