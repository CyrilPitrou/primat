# API reference

Generated from primat's docstrings, one page per module a programmatic user
interacts with directly; each opens with what the module is for and which
how-to guide covers the same ground for a reader with a task rather than a
name. The rest of the package is browsable through the source links.

Start at {doc}`backend` (`run_bbn`, the main entry point) and
{doc}`../parameters` (what you can pass it).

```{toctree}
:maxdepth: 1

backend
primat
config
evolution
plotting
sensitivity
weak_rates
background
network_data
neutrino_history
```
