# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
*.so


# macOS
.DS_Store

# Editors
.vscode/

# Stray backup files
*.restored
*~

# primat output
output_time_evolution.tsv
results/
notebooks/plots/
notebooks/results/

# One-off scratch scripts / studies, not part of the package or test suite.
scratch/

# graphify knowledge-graph cache (local-only)
graphify-out/

# Local-only files: not committed to the repository
CLAUDE.md
AGENTS.md

# Local Claude Code tooling settings
.claude/

# Local Codex CLI tooling settings (machine-specific paths, e.g. hooks.json)
.codex/

# Runtime-generated n<->p weak-rate cache files (fingerprinted, one per
# background config). The shipped set under primat/data/weak/ stays tracked
# (already committed); this only ignores new files a local run regenerates.
primat/data/weak/nTOp_*.txt

# Local planning notes, not tracked
FABLEPLAN.md
